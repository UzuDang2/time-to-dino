.DS_Store

# Claude Code 워크트리 (메인 레포와 분리된 작업 트리 — 커밋 대상 아님)
.claude/worktrees/

# 데이터 파이프라인 로컬 캐시 (재생성 가능)
data/.sheet_cache.xlsx

# 서비스 계정 키 (커밋 절대 금지)
.secrets/
*.sa.json
*-sa.json
service-account*.json
google-credentials*.json
