# pending.md

남은 과제와 대기 중인 외부 액션. 항목 끝의 `[요한]` / `[디렉터]` / `[공동]`은 담당.

---

## 대기: 요한 측 QA (12차 세션 — 2026-04-24, D-72~D-78 사냥감 확장 2·3단계)

D-72~D-78 구현 완료 (8커밋 분할). 시트 4탭 편집(방어구 신설 / 조합레시피 5건 / 전투카드 defense 컬럼+2카드 / 사냥감 actions_per_turn+defense 컬럼+L2 7종 업데이트). `localhost:8765` 로드 후 아래 체크.

- [ ] **[요한]** 🧪 **QA: D-72 방어구 시스템 (5종 + 조합)**
  - 새 게임 → 줄기 3개 보유 → 합성 패널에 `줄기 + 줄기 + 줄기 → 잎사귀 조끼` 노출 → 확정 → 인벤에 1x1 armor(defense 1) 생성.
  - 목재 2 + 끈 1 → 나무 방패(defense 2). 깨끗한 천 2 + 끈 1 → 천 갑옷(defense 2). 나무 방패 + 돌맹이 2 → 강화 방패(defense 3). 천 갑옷 + 돌맹이 2 → 비늘 갑옷(defense 3).
  - 인벤에 shield 아이템이 있으면 전투카드 `방패 막기` 덱에 포함. armor만 보유면 제외. 카드 🛡️ 배지에 인벤 합산 defense 반영.

- [ ] **[요한]** 🧪 **QA: D-73~D-74 방어 카드 + HP 차감**
  - 전투 덱에 `웅크리기(🛡️+1, 항상 사용 가능)` 상시 노출.
  - `방패 막기`: requirement DSL 'shield'가 카테고리 매칭으로 동작 — 방패 보유자만 등장. 🛡️ 배지에 카드+방어구 합산치 표시 (예: wooden_shield + leaf_vest → 🛡️+4).
  - Level 2 prey 조우 → 4슬롯 전투. prey 공격 턴에 유저 카드 defense만큼 받는 대미지 감쇄. HP 0 → 게임오버(사인 'health').
  - 전투 후 방어구 소모 없음(내구도 없음).

- [ ] **[요한]** 🧪 **QA: D-75 2등급 4턴 행동 패턴**
  - 각 L2 prey 7종(boar/dinosaur/deer/badger/fox/turkey/armadillo) 조우 → 적 행동 슬롯에 `공격/방어/회피/눈치` 라벨 + (회피 턴만) 회피율.
  - 행동 효과 검증: attack=공격(유저 HP↓), defend=유저 공격 -1 감쇄, evade=기존 회피 판정, peek=회피 0% 즉 명중 보장.
  - 전투 로그에 preyDamage(N) / preyDefended(N) 표기.

- [ ] **[요한]** 🧪 **QA: D-76 L2 5마리 스폰 + 거리**
  - 게임 시작 → `window.__preys` 총 10마리 (L1 5 + L2 5). L2 5마리 상호 BFS 거리 ≥ 3.
  - L2 habitat 매칭: boar(덤불,숲) / dinosaur(동굴) / deer(평원,숲) / badger(덤불) / fox(숲) / turkey(평원) / armadillo(동굴).
  - `PREY_SPAWN = {level1:5, level2:5}` 상수 하드코딩 경로 — 시트 이관 전이라 index.html에서만 조정 가능.

- [ ] **[요한]** 🧪 **QA: D-77 보스 포식 이동 우선권**
  - 보스가 L2 prey 타일거리 2 이내 진입 → 포식 경로 시작. 플레이어 이동 1회마다 BFS 첫 홉씩 타겟 추적. 도달 → prey 제거 + 2턴 정지. 정지 종료 시 해당 타일에 🦴 사체 마커.
  - 포식 중 플레이어가 그 타일에 진입 → 기존 보스 조우 게임오버(사인 'boss') 그대로.
  - 포식 이동은 일반 모드(2턴마다)/추격 모드(매 턴) 공통으로 우선 적용. L1 prey는 포식 대상 아님.

- [ ] **[요한]** 🧪 **QA: D-78 사체 타일 🦴 + 귀기울이기 + 뒤져보기**
  - 포식 완료된 타일 → visited 시 중앙 🦴 표시. 미방문 상태에선 노출 안 됨.
  - 인접 타일에서 `귀 기울이기` → 사체 인접이면 carcassTrace에 추가, 해당 타일 👣 대신 🦴. 메시지 "어디선가 짐승의 사체 냄새가 풍긴다."
  - 사체 타일 `뒤져보기` 첫 사용 → 일반 region 드롭 + 생고기 +1 보너스. 두 번째 사용부터는 일반 드롭만(carcassMeatTaken 기록).

- [ ] **[요한]** 🧪 **QA: D-72~D-78 회귀**
  - D-71 회귀: 게/메뚜기 체인, 꼬치 요리, drop_item 경로 정상.
  - 기존 전투카드 7장(주먹/돌던지기/창찌르기/창던지기/회피/도망/새총쏘기) 회귀 — defense 빈값=0 폴백, slotLimit/accuracy 동작.
  - L1 prey 9종 3턴 전투 회귀 (evade_per_turn 그대로).
  - 합성 패널, 드롭풀, 머지 경로 회귀.

---

## 대기: 요한 측 QA (12차 세션 — 2026-04-24, D-71 사냥감 확장 1단계)

D-71 구현 완료 (3커밋). 시트 3탭 편집 + 파이프라인/런타임 업데이트. 로컬 `localhost:8765` 로드 후 아래 체크.

- [ ] **[요한]** 🧪 **QA: D-71 게 보상 체인 (핵심 시나리오)**
  - 새 게임 → 시냇물 타일에서 게 사냥 성공 → 인벤에 `게(crab_whole)` N개(prey.meat 수만큼) 획득. LootToast 이름 **"게"**로 표시(기존 "생고기" 아님).
  - 인벤에서 게 단독 탭 → 효과 없음(카테고리=재료, disposable=N).
  - 돌맹이 획득(동굴/평원 뒤져보기) → 돌 + 게 드래그 → 합성 패널에 "돌맹이 + 게 → 발라낸 게살 x2" 노출 → 확정 → 인벤에 **게살 2개** 생성 + 메시지 "재료를 조합해 발라낸 게살 2개를 만들었다."
  - 인벤 포화 상태(가방 1칸만 빈)로 같은 조합 시도 → 게살 1개만 생성 + "가방이 가득 차 1개는 두고 왔다." 안내.

- [ ] **[요한]** 🧪 **QA: D-71 게살 꼬치 + 요리 2단계**
  - 게살 보유 상태에서 단독 섭취 → hunger+1.
  - 숲에서 나뭇가지 획득 → 게살 + 나뭇가지 합성 → **게살꼬치** (2단계 음식).
  - 휴식 카드 사용 → [🔥 요리하기] 버튼 활성 → CookingModal에 "게살꼬치 → 구운게살꼬치" 카드 노출 → 요리 성공 → **구운게살꼬치** 획득 (3단계).
  - 구운게살꼬치 섭취 → hunger+2, health+1 (휴식 scale 반영 검증).

- [ ] **[요한]** 🧪 **QA: D-71 메뚜기 단일 드롭**
  - 평원 메뚜기(grasshopper) 사냥 성공 → 인벤에 `메뚜기(grasshopper_whole)` N개. LootToast 이름 **"메뚜기"**.
  - 메뚜기 단독 섭취 → hunger+1 (산딸기와 동일 티어, 조합 경로 없음).

- [ ] **[요한]** 🧪 **QA: D-71 회귀 (나머지 14종 prey)**
  - 토끼·쥐·다람쥐·새·도롱뇽·뱀·개구리(1등급 7종) 사냥 → 전부 **생고기** 드롭 유지(drop_item 공란 → meat 폴백). 메시지 "사냥에 성공했다. 생고기 N을(를) 얻었다."
  - 2등급 prey(여우·큰 새·아르마딜로·늑대·보어·사슴·뱀) — drop_item 공란 → 생고기 폴백(2단계 전투 자체는 다음 이터레이션).
  - 기존 생고기 + 나뭇가지 → 생고기꼬치 조합, 요리 → 고기꼬치구이 경로 정상(D-57 회귀 없음).
  - 기존 합성 패널·머지·드롭·LootToast 회귀 없음.

- [ ] **[요한]** 🧪 **QA: D-71 result_count 시트 스키마 안정성**
  - `make data` 재실행 후 `data/combos.json` 재생성 → `stone+crab_whole→crab_meat` 레시피에 `"count":2` 필드 유지.
  - 기존 레시피(빈 count)는 `count` 필드 없음 — 파이프라인 기본값 1 폴백 정상.

---

## 대기: 요한 검수 — 사냥감 확장 2·3단계 (D-71 후속)

> D-71은 1단계(게·메뚜기 보상 다양화)만 담았다. 2·3단계는 **디렉터 기본값**으로 아래 정리 — 요한 검수 후 구현 착수.

### 2단계 — 2등급 사냥감 전투 + 방어 시스템

- [ ] **[요한 검수]** Q2 — prey `attack` 값: 기본값 **b=2** (현재 0 유지 prey들은 0 그대로). level=2 prey 중 `turkey(큰 새)` 1 이미 값 있음 → 유지. 나머지 level=2 공격형 prey에 2 할당.
- [ ] **[요한 검수]** Q3 — **방어 +1 신규 defend 카드** 도입. 100% 성공, requirement 없음. base_cards 또는 전투카드 공통 풀.
- [ ] **[요한 검수]** Q4 — **공통 카드풀**: 사냥 전투와 보스 전투가 같은 defend 카드 공유. 방어 효과 = "적 공격 1 감소"(누적 아님, 슬롯 단위).
- [ ] **[요한 검수]** Q5 — **5마리 별도**(level=2 prey 7종 중 5마리 대상). 어떤 5마리인지 요한 선택 — 디렉터 기본: fox/turkey/wolf/boar/deer.
- [ ] **[요한 검수]** Q6 — **2등급만** 적용. waypoint 이탈(사냥감이 habitat 밖 추격), **2회 정지**(플레이어 근접 시 도주 대신 공격).

### 3단계 — 보스 포식

- [ ] **[요한 검수]** Q7 — 보스 사냥감 포식 시 **생고기 확정 + 무한 드롭**(발각률 급등 리스크와 트레이드오프). prey 제거 + 인벤 가용칸 전부 생고기로 채움.
- [ ] **[요한 검수]** Q8 — 보스 포식 전체 스펙 OK (디렉터 기본값 수용 예정, 요한 최종 사인 필요).

---

## 대기: 요한 측 QA (12차 세션 — 2026-04-24, D-60/D-61/D-62)

D-60/D-61/D-62 구현 완료 (5 feat 커밋 + 1 data 커밋). 로컬 `localhost:8765` 로드 후 아래 체크.

- [ ] **[요한]** 🧪 **QA: D-60 턴별 회피율 (핵심 시나리오)**
  - 새 게임 → 토끼(evade_rate=30, `30,30,20`) 조우 → HuntCombatModal.
  - 초기 적 행동 슬롯 — T1/T2: `회피 30%`, T3: `회피 20%` (기본색).
  - 메뚜기 조우 → T1/T2: `회피 50%`, T3: `회피 30%`.
  - 게(evade_rate=20, `20,20,10`) → T1/T2: `회피 20%`, T3: `회피 10%`.
  - Level 2 사냥감(보어·사슴 등, evade_per_turn 빈값) → T1/T2/T3 모두 evade_rate 그대로 (폴백).
  - 창으로 찌르기(accuracy 20+) 배치 시 해당 턴 회피율이 base에서 accuracy만큼 추가 차감.

- [ ] **[요한]** 🧪 **QA: D-60 도주 누적 패널티**
  - 토끼 전투 시작 → 일부러 도주(run_away 성공 혹은 3턴 소진 prey_fled).
  - prey 상태 콘솔 확인: `fleeCount: 1` (React DevTools 또는 window 디버깅).
  - 동일 토끼 재조우 → T1/T2: `회피 20%`, T3: `회피 10%` (각 10%p 하향).
  - 두 번 도망간 토끼 재조우 → T1/T2: `회피 10%`, T3: `회피 0%` (Math.max(0,…) 하한).
  - victory / player_fled 분기는 fleeCount 증가 안 함.
  - 턴 로그의 `effectiveEvade` 값이 UI의 base - accuracy와 **일치**.

- [ ] **[요한]** 🧪 **QA: D-61 맵 크기 11x11**
  - 새 게임 → 맵이 121 타일(11x11) 크기로 렌더.
  - 빈 슬롯은 24~37개 범위, 특수 타일(good≈10, trap≈5), 보스가 플레이어에서 7칸 이상 거리에 스폰.
  - 시작 시 useLayoutEffect로 현재 위치가 뷰포트 중앙에 오는지(기존 동작 유지).
  - 스크롤해도 원형 패딩 느낌이 자연스러움 (PADDING_HEXES=7 유지).
  - 사냥감이 habitat에 맞게 분포(맵 확대로도 스폰 문제 없음).

- [ ] **[요한]** 🧪 **QA: D-62 보스 미방문 우선 배회**
  - 새 게임 → 일부러 detection 낮게 유지(일반 모드) → 30~50턴 이동하며 보스 위치 관찰.
  - 결과 화면 [🗺️ 맵 보기] → 보스 경로(B1, B2, …)가 같은 타일에 연속 찍히지 않음 (미방문 타일 우선).
  - 11x11 맵 넓은 영역에 걸쳐 보스 경로가 분산되는지 체감.
  - 발각률 80% 돌파 → chaseMode 전환 → 플레이어 접근이 최우선인지(기존 로직 유지, 미방문 필터 영향 없음).
  - 막다른 골목(연결 1개)만 있으면 전부 방문해도 거기로 이동(폴백 정상).

- [ ] **[요한]** 🧪 **QA: D-60/D-61/D-62 회귀**
  - 기존 사냥 전투 흐름(승리/도망/player_fled) 정상, 무기 내구도·accuracy 합산·auto-fail 무기부재 회귀 없음.
  - 맵 생성 후 시작 타일·탈출구 연결 보장(`_isValidMap` 통과율 정상).
  - 결과 화면 맵 보기(D-53) 정상 동작.

---

## 대기: 요한 측 QA (12차 세션 — 2026-04-24, D-58 HuntModal 배지 통일)

D-58 구현 완료 (3커밋). `index.html` + `gameStyles.css`. 로컬 `localhost:8765` 로드 후 아래 체크.

- [ ] **[요한]** 🧪 **QA: D-58 카드 배지 통일 (내 슬롯 / 내 손패 공통)**
  - 사냥감 타일 도착 → 사냥시작 → HuntCombatModal 오픈.
  - 손패 각 카드 기대 배지 (순서 고정: 🗡️ → % → 🏹, 줄바꿈 없음):
    - 주먹으로 치기: `🗡️+1` `100%` (🏹 없음 — accuracy=0)
    - 돌 던지기(돌맹이 보유): `🗡️+2` `90%` `🏹+20`
    - 창으로 찌르기(나무창 보유): `🗡️+4` `100%` `🏹+N` (N = 20 + 무기 accuracy)
    - 회피하기: `75%` (🗡️·🏹 둘 다 없음 — damage=0)
    - 도망치기: `25%`
  - 기존 `공N` / `명중 +N%` 텍스트 라벨 **완전히 사라짐**.
  - 내 슬롯에 배치한 카드도 동일 형식으로 표시.
  - 내구 배지(`내구 N/M`)는 예전처럼 별도 줄, 노란색 그대로.

- [ ] **[요한]** 🧪 **QA: D-58 적 슬롯 최종 회피율 실시간 반영 (핵심 시나리오)**
  - 토끼(evade_rate=30) 조우 → HuntCombatModal.
  - 초기 적 행동 슬롯 T1/T2/T3 모두 `회피 30%` 기본색 표시.
  - T1에 **창으로 찌르기**(accuracy 20 + 무기 보정 포함, 최소 20) 드롭 → T1 즉시 `회피 10%` **초록색 + bold**.
  - T2에 **주먹으로 치기** (accuracy=0) 드롭 → T2는 `회피 30%` **기본색 유지** (감소 없음).
  - T3에 **회피하기**(damage=0) 드롭 → T3는 `회피 30%` **기본색 유지**.
  - T1 카드 해제(슬롯 클릭) → 즉시 `회피 30%` 기본색 복구.
  - 확정 → 턴 로그의 `effectiveEvade` 값과 배치 단계 표시값 **일치**.
  - 새총 + 돌맹이처럼 accuracy 합산값이 baseEvade보다 큰 케이스: `회피 0%` 초록+bold (음수 방지 max 동작).

- [ ] **[요한]** 🧪 **QA: D-58 회귀**
  - `resolveHunt` 결과(명중·회피·데미지·HP) 이전과 동일 — 렌더만 바뀌었음을 확인.
  - D-51 내구 배지(`내구 N/M`), ∞/×N 뱃지, 손패 고갈(`exhausted` 회색) 모두 정상.
  - 풀 세션(5장 카드 각 배치) 시 overflow 없음, 배지가 한 줄 안에 들어감(좁은 폭에서도 `inline-flex` 유지).

---

## 대기: 요한 측 QA (12차 세션 — 2026-04-24, G/H/I 기획 일괄)

D-55/D-56/D-57 구현 완료. 커밋 시리즈는 `git log main` 최근 5건 참조.
로컬 `localhost:8765` 로드 후 아래 E2E 체크.

- [ ] **[요한]** 🧪 **QA: D-55 스탯 경고 점멸**
  - 새 게임 → 시간 경과 + 함정 등으로 `hunger ≤ 3` 유도 → 배고픔 게이지 채움 부분만 빨간 점멸 (0.6s alternate).
  - 라벨(❤️/🍖), 숫자(3/12), 게이지 배경은 정상 표시.
  - `health ≤ 2`도 동일 (상처 누적 시나리오).
  - 회복 후 임계 초과(hunger=4, health=3) 시 점멸 즉시 멈춤.

- [ ] **[요한]** 🧪 **QA: D-56 보스 결과화면 + 점프스케어**
  - 발각률 80% 초과 → 보스 추격 모드 유도 → 같은 타일 진입.
  - 기대 순서:
    1. 이동 직후 **BossJumpscare 0.55s 재생** (화면 중앙 공룡 확대).
    2. 점프스케어 종료 후 GameEndModal 오픈 — "💀 게임 오버" + subtitle "보스가 당신을 삼켰습니다" + 가이드.
    3. 모달에 [🗺️ 맵 보기] + [다시 시작] 버튼. 맵 보기 클릭 → 보스 이동 경로(B1, B2…) + 플레이어 경로 확인 가능.
    4. 플로팅 바 왼쪽 문구 "← 결과 화면" (승리일 때만 "← 승리 화면").
  - 체력 사망/배고픔 사망에서도 [🗺️ 맵 보기] 노출 확인 (기존엔 victory 전용이었음).
  - listen 거리 1 점프스케어(보스 인접, 게임오버 없음)는 기존대로 연출만 — 회귀 없음.

- [ ] **[요한]** 🧪 **QA: D-57 요리 시스템 (핵심 시나리오)**
  - 사냥감 처치로 생고기 1 + 뒤져보기로 나뭇가지 1 확보.
  - 인벤에서 생고기 ↔ 나뭇가지 드래그 → 합성 패널에 "생고기 + 나뭇가지 → 생고기꼬치" 노출 → 조합 성공.
  - 휴식 카드 사용 → 휴식 모달 상단에 **[🔥 요리하기]** 버튼 노출 (생고기꼬치 보유 상태 → 주황색 활성).
  - 요리 버튼 클릭 → CookingModal 오픈 → "생고기꼬치 → 고기꼬치구이 (보유 1)" 카드 + [🔥 요리하기] 활성.
  - 클릭 → "고기꼬치구이을(를) 구웠다." 메시지 + 인벤에 고기꼬치구이 1개(생고기꼬치 사라짐) + 요리 모달 닫힘.
  - 휴식 모달로 복귀 → 고기꼬치구이를 선택 → hunger+3, health+2 (휴식 카드 recover scale/bonus 반영 계산).
  - **회귀**: 기존 휴식 모달 일반 음식 선택(산딸기/버섯모둠/딸기모둠 등) 정상 작동.

- [ ] **[요한]** 🧪 **QA: D-57 요리 불가 상태**
  - 꼬치 재료가 전혀 없는 상태에서 휴식 카드 사용 → [🔥 요리하기] 버튼 **회색 비활성**(클릭 무반응).
  - 물고기꼬치만 보유 → CookingModal에 "물고기꼬치 → 생선꼬치구이"만 활성, "생고기꼬치 → 고기꼬치구이"는 "재료부족" 회색.

- [ ] **[요한]** 🧪 **QA: D-57 회귀 (바베큐 폐지)**
  - 생고기 + 나뭇가지 2 또는 생고기 + 목재 1 보유 시 **더 이상 휴식 모달에 🍖 바베큐 가상 선택지가 뜨지 않아야 함** (꼬치 조합 → 요리 2단계만 경로).
  - 합성 패널 동작 회귀 없음 (다른 레시피 정상 노출).
  - 인벤 머지·조합·드롭·LootToast·listen 내레이션 모두 정상.

---

## 대기: 요한 측 QA (11차 세션 — 2026-04-24, 사냥 전투 B/C/D)

D-50/51/52 구현 완료. 커밋 히스토리는 `git log main` 최근 5건 참조. 로컬에서 E2E 확인 부탁.

- [ ] **[요한]** 🧪 **QA: D-50 명중률(accuracy) 배지 노출**
  - `make data` 실행 후 `localhost:8765` 로드 → 새 게임.
  - 사냥감 타일 도착 → 사냥시작 → 전투 모달 오픈.
  - 손패 카드별 기대 배지:
    - 돌 던지기(돌맹이 보유 시): 공2 90% **명중 +20%** (무기 X)
    - 창으로 찌르기(나무창 보유 시): 공2 100% **명중 +40%** (30 + 무기 10)
    - 창던지기(나무창 보유 시): 공4 100% **명중 +30%** (20 + 무기 10)
    - 새총 쏘기(새총+돌맹이 보유 시): 공3 100% **명중 +180%** (90 + 무기 90) — 큰 수지만 clamp 없이 그대로. effectiveEvade=0으로 항상 명중 통과.
  - dodge/run_away에는 배지 **안 뜸** (accuracy=0).

- [ ] **[요한]** 🧪 **QA: D-51 내구도 시스템**
  - weapon_basic 현장 획득(시트에선 stone x2 + wood + plant_fiber 조합). 인벤 탭 → 아이템 정보창에 "내구도 5/5" 표시.
  - 전투에서 창으로 찌르기 슬롯 배치 → 확정 → 성공 시 "내구도 4/5"로 감소.
  - 다음 전투 진입 시 손패 배지 "내구 4/5" 표시.
  - 나무창 5회 사용 → 인벤에서 사라짐 + "나무창 파손" 로그.

- [ ] **[요한]** 🧪 **QA: D-51 창던지기 실시간 재고 (핵심 시나리오)**
  - 나무창 1자루 + 돌맹이 넉넉히 보유 상태 진입.
  - 슬롯 3개에 **창던지기 x3** 배치 → 확정.
  - 기대: 1턴차 창던지기 → 성공 시 "데미지 4 (HP X)" + 나무창 즉시 인벤 제거.
    2·3턴차 창던지기 → "무기가 없어 실패" 로그.
  - 전투 종료 후 인벤에 나무창 없음.

- [ ] **[요한]** 🧪 **QA: D-52 새총 조합**
  - 가방에 나뭇가지 + 끈(plant_fiber) 보유 → 합성 패널에서 `새총` 레시피 노출 → 조합 성공 → 1x1 새총 아이템 획득 + 내구도 3/3.
  - 새총만 보유(돌맹이 없음) → 사냥 전투 손패에 slingshot_shot **미노출**.
  - 새총 + 돌맹이 보유 → 손패에 노출, 사용 성공 시 돌맹이 1개 차감 + 새총 내구도 -1.
  - 새총 3회 사용 → 새총 인벤에서 사라짐.

- [ ] **[요한]** 🧪 **QA: 회귀**
  - 기존 D-46 사냥 플로우(주먹/회피/도망/돌던지기 전용), D-47 합성 패널, D-48 3턴 전투 모두 정상.
  - 보스 게임오버, listen 내레이션, 인벤 머지/조합, LootToast 회귀 없음.

## 대기: 요한 측 QA (11차 세션 E/F — 2026-04-24, 결과 화면 + 스크롤 패딩)

D-53/54 구현 완료. 커밋 `fbcc605`, `b47438c` 참조.

- [ ] **[요한]** 🧪 **QA: D-53 결과 화면 보스 경로**
  - 새 게임 → 플레이어가 탈출구까지 이동해 승리.
  - GameEndModal에서 [🗺️ 맵 보기] 클릭.
  - 기대:
    - 플레이어 방문 타일에 기존 노란 동그라미 + 방문 순서(1, 2, 3...) 왼쪽에 표시.
    - 보스가 실제 이동한 타일에 **빨간 동그라미 + B1, B2...** 오른쪽에 표시 (같은 타일을 여러 번 지나가도 첫 진입만 번호 부여).
    - 보스 시작 위치가 B1.
    - 플레이어와 보스가 같은 타일에 있었으면 노란 원(좌) + 빨간 원(우) 둘 다 보임.
  - [← 승리 화면] 버튼으로 GameEndModal 복귀, [다시 시작] 정상 동작.

- [ ] **[요한]** 🧪 **QA: D-53 전체 맵 윤곽**
  - 같은 [맵 보기] 모드에서 **미방문 타일**도 반투명 회색 hex로 노출되는지 확인.
  - 미방문 타일은 클릭해도 이동/어떤 반응도 없어야 함(cursor도 변화 없음).
  - 미방문 타일에는 지역 이름·prey·👣 흔적 **모두 안 보임** (스포일러 방지).
  - 맵 전체 크기를 한눈에 가늠 가능해야 함 (빈 슬롯은 그대로 안 보임).

- [ ] **[요한]** 🧪 **QA: D-54 스크롤 패딩**
  - 새 게임 로드 → 플레이어 타일이 뷰포트 중앙에 오는지(기존 동작 유지).
  - 맵을 상/하/좌/우 끝까지 스크롤 → **어느 방향이든 빈 공간이 넉넉히 이어지고 "경계"를 느낄 수 없어야 함**.
  - 모바일 뷰포트(Chrome DevTools 기기 모드)에서도 플레이 영역 답답함 없음.
  - 기존 7x7 타일 외에는 아무것도 렌더되지 않아야 함(원형 테두리·배경 패턴 등 노출 X).

- [ ] **[요한]** 🧪 **QA: E+F 회귀**
  - D-47 초기 중앙 정렬, D-48 플레이어 이동 경로 노란 번호, D-50 피스(말) 애니메이션 모두 정상.
  - listen → 보스 toast 정상, 점프스케어 정상.
  - 탈출 승리 외에도 체력/배고픔/보스 사망 시 GameEndModal 정상 노출(맵 보기 버튼은 승리에만).

---

## 대기: 요한/Mac 로컬 측 작업 (10차 세션 — 웹 세션 2026-04-23)

> 🌐 이 블록은 **claude.ai/code 웹 환경(Linux 샌드박스)**에서 작업된 내용이다.
> 웹 세션은 Notion MCP 미연결 + `*.notion.site` 방화벽 차단 상태였기 때문에
> SSOT(시트/Notion) 직접 편집이 불가능. repo 내 JSON만 수정한 변경이 포함되어 있다.
> **Mac 로컬 세션에서 이 블록을 읽고, 아래 시트/파이프라인 정리 항목을 처리해야 한다.**

### 이번 세션 완료 (웹, 커밋 `a4a5688` + 후속 UI 이터레이션 → main)

- [x] **[디렉터/웹]** ✅ **알림(토스트) 페이드아웃 속도·이징 재조정**
  - LootToast / TileEventToast: 기존 `2s ease-in` → **0.25s `cubic-bezier(0.075, 0.82, 0.165, 1)` (easeOutCirc)**.
  - 요한 지시: "아웃서클 그래프"로 빠르게 꺼지게.

- [x] **[디렉터/웹]** ✅ **listen(귀 기울이기) 보스 위치 시각화 — 하단 텍스트 → 공룡 토스트**
  - 기존 `setMessage(... 보스는 타일 #N ...)` 텍스트는 시인성 떨어진다는 피드백.
  - 새 패턴: `showTileEventToast({ emoji: '🦖', title, message, color })` — 거리별 테두리 색.
    - 거리 1: `#ff3b3b` (빨강) + 🦖 점프스케어
    - 거리 2~3: `#ffb300` (노랑)
    - 거리 ≥4: `#4CAF50` (초록)
  - 서사 톤의 기존 내레이션(가쁜 숨소리…/발걸음 소리…)은 토스트 message에 그대로 보존.

- [x] **[디렉터/웹]** ✅ **보스 인접(1칸) 시 점프스케어 연출**
  - `BossJumpscare` 신규 컴포넌트 + `gameStyles.css` 점프스케어 키프레임(overlay + 아이콘 팝). 0.55s 단일 재생 후 자동 언마운트.
  - 공룡 아이콘이 화면 중앙에서 scale 0.3 → 12로 급격히 확대되다 사라짐. radial-gradient 빨간 배경이 동시에 페이드.

- [x] **[디렉터/웹]** ✅ **승리(탈출 성공) 시 [맵 보기] 모드 + [다시 시작] 플로팅**
  - `moveHistory` state(방문 순서 배열) + `showPathView` state 도입.
  - `initializeGame`에서 `[playerStart]`로 초기화, `moveTo`에서 targetId push.
  - `GameEndModal`에 `victory`일 때만 🗺️ 맵 보기 버튼 추가. 클릭 시 모달 숨기고 맵 뷰 진입.
  - 맵 보기 중 각 방문 타일 좌측에 순서 번호(첫 방문 기준, indexOf).
  - 하단 고정 플로팅 바: [← 승리 화면] / [다시 시작] — 양방향 이동 가능.

### 이번 세션 완료 (웹, 커밋 `a4a5688` → main, 이전 라운드)

- [x] **[디렉터/웹]** ✅ `사냥시작(hunt_start)` 카드 잔존 버그 수정 (`index.html`)
  - 원인: `useCard('hunt_start')`의 `!prey` early return이 카드를 손패에 남겨두고 종료. prey가 처치·도망으로 사라져도 자동 정리 로직이 없었음. 이동하지 않고 머무는 동안 계속 손에 남음.
  - 조치:
    1. `preys`/`currentTile` 감시 useEffect 추가 — 현재 타일에 해당 preyId가 없으면 손패에서 즉시 무덤행.
    2. `useCard('hunt_start')` early return 경로에도 `setHand(... filter)` + `setGraveyard(...card)` 보강(방어적).

- [x] **[디렉터/웹]** ✅ 동굴·평원 돌멩이 획득 확률 상향 (`data/drop_table.json` + `data/data.js`)
  - 현재 JSON 상태: `weights.동굴.env = [70, 30]`, `weights.평원.env = [70, 30]` 추가 (stone:다른재료 = 7:3).
  - 효과: 동굴 stone 확률 **27.5% → 38.5%** (+11%p), 평원 **20% → 28%** (+8%p).

### 🚨 Mac 로컬 후속 (SSOT 불일치 해소)

- [ ] **[요한/로컬]** ✍️ **시트 `드롭테이블`/`드롭풀` 또는 weights 영역에 동굴·평원 env 가중치 70:30 반영**
  - **이슈**: 현재 시트에는 `weights` 구조가 `시냇물.food` 하나만 존재. `동굴/평원.env`는 JSON에 직접 적었기 때문에, 다음 `make data` 실행 시 시트 값으로 **덮어쓰여 롤백될 위험**이 있다.
  - **조치 옵션 A (권장)**: 시트에 weights 탭/컬럼 체계가 어떻게 잡혀 있는지 로컬 세션에서 `scripts/fetch_data.py` 확인 후, 동굴/평원 env 가중치 행을 시트에 추가 → `make data` → diff로 `drop_table.json`이 같은 값으로 유지되는지 확인 후 커밋.
  - **조치 옵션 B**: 파이프라인에 weights 시트 탭이 없다면, `fetch_data.py`에 weights 시트 파싱 추가 구현. 시트 구조는 가벼운 `(지역, 카테고리, 인덱스, 가중치)` 4열이면 충분.
  - **빠른 체크**: `make data` 후 `data/drop_table.json`의 `weights.동굴`/`weights.평원` 키가 사라지면 SSOT 반영이 안 된 것.

- [ ] **[요한/로컬]** ✍️ **(선택) 현 세션 범위 Notion 기록이 필요하면 design-decisions.md에 D-XX 소급 append**
  - 이번 변경은 밸런스 조정 + 버그 픽스라 별도 D번호 없이 커밋 메시지만 남김.
  - 만약 `동굴/평원 돌멩이 가중치`가 장기 원칙이면 D-XX로 정식화 가능.

---

## 대기: 요한 측 작업 (9차 세션 — D-46 사냥감 전투 1단계)

- [ ] **[요한]** 🧪 **QA: D-46 사냥감 전투 1단계**
  - `localhost:8765` 로드 → 새 게임 시작.
  - **스폰**: 콘솔 `window.__preys` 에 5마리 배열 확인. 각 prey의 tileId가 habitat region에 맞는지.
  - **흔적(listen)**: 사냥감이 인접한 타일에서 귀 기울이기 사용 → "근처에서 희미한 기척이 느껴진다." 메시지 + 인접 타일에 👣 표시.
  - **조우**: 이동 중 사냥감 타일 도착 → 🐰 등 아이콘 노출 + 손패에 "사냥시작" 카드 자동 추가(overflow 카드, 5/5 카운터에 미포함).
  - **이탈**: 사냥감 없는 타일로 이동 → 사냥시작 카드 자동 제거.
  - **전투**: 사냥시작 카드 클릭 → 전투 모달 열림 → 손패 카드 탭(가장 왼쪽 빈 슬롯에 배치) → 슬롯 카드 탭(손패로 복귀) → 3슬롯 채움 → 확정.
  - **승리**: HP 0 → 생고기 지급 + LootToast + 메시지 "사냥 성공. 생고기 N을(를) 얻었다."
  - **도주(3턴 빠르게 도망)**: 3턴차 prey 탈출 → 인접 타일로 이동 + 원 타일에 발자국(방향 화살표) + "사냥감이 도망쳤다. 발자국이 남아있다."
  - **포기(run_away 성공)**: "사냥을 포기하고 물러섰다." prey 제자리.
  - **회귀**: 보스 게임오버·listen 4단계 메시지·뒤져보기·머지/조합·LootToast 그대로.

> 💡 9차 세션 시트 pending 4건(전투카드 count+수치+throw_spear, 사냥감 habitat, 특수카드 탭 신설, 생고기 Notion 등재)은 2026-04-24 디렉터가 gspread API로 일괄 반영 완료. 아래 "완료" 섹션 참조.

---

## 대기: 요한 측 작업 (6차 세션 — D-31 listen 내레이션 + D-32 머지 UX 간소화)

- [ ] **[요한]** 🧪 **QA: D-31 귀 기울이기 거리별 메시지**
  - `localhost:8765` 로드 → 이동하면서 listen 카드 뽑히는 순간 클릭.
  - 보스와 거리 1/2/3/≥4일 때 각 메시지 확인:
    - 1칸: "가쁜 숨소리가 바로 곁에서 들려온다. 보스는 타일 #N에 있다." (+ 인접 타일 revealed 갱신)
    - 2칸: "숨쉬는 소리가 꽤 가까이 들리는 듯 하여 소름이 돋는다."
    - 3칸: "발걸음 소리가 가까워진듯 하다."
    - 4칸 이상: "발걸음 소리가 아주 멀리서 들린다, 당분간은 안심해도 될 것 같다."
  - 거리 ≥2 구간에서 타일이 revealed로 노출되면 안 됨(청각 단서만).

- [ ] **[요한]** 🧪 **QA: D-32 롱프레스 + 짧은 탭 머지 UX**
  - 가방에 산딸기 2개 확보 → 롱프레스로 첫 산딸기 선택 → 두 번째 산딸기 **짧게 탭** → 딸기모둠 1개로 축약 확인(확정 버튼 안 눌러야 함).
  - 서로 다른 재료(식물 섬유+질긴줄기 등) 짧은 탭 시 즉시 조합 결과물 생성 확인.
  - 기존 드래그·회전·확정 버튼 플로우 회귀 없는지 한 번씩 확인(빈 칸에 옮기기, 같은 위치로 되돌리기).
  - 머지/조합 불가 쌍(돌맹이+나뭇가지)을 짧게 탭 = 선택이 두 번째 아이템으로 전환(기존 swap 로직 유지).

## 대기: 요한 측 작업 (5차 세션 — D-24 머지·조합 통합)

- [ ] **[요한]** 🧪 **QA: D-24 머지·조합 통합 시스템**
  - `localhost:8765` 로드(이미 서버 떠있음) → 아래 항목 확인:
    - 뒤져보기로 **나뭇가지**가 가방에 들어오면 (숲 타일 필요), 두 개 확보 후 인벤토리 열기.
    - 한 나뭇가지를 롱프레스로 선택 → 다른 나뭇가지 칸으로 드래그 → 미리보기 **노란 점선(머지)** + 확정 버튼에 `확정 (머지)` 표시.
    - 확정 → 상단 메시지 "두 재료를 합쳐 목재을(를) 얻었다." + 인벤에 **목재** 1개(나뭇가지 2개 사라짐).
    - 서로 다른 재료(질긴줄기 + 질긴줄기로 끈 만든 뒤 질긴줄기 또 하나) → 끈+질긴줄기 오버랩 시 **보라 점선(조합)** + `확정 (조합)`. 결과 "깨끗한 천".
    - 돌맹이 2개 겹치기 → **파란 점선(교체/swap)** — 머지·조합 둘 다 비활성. 돌맹이는 2단계 상위가 없음.
  - QA 이상 있으면 디렉터에게.

- [ ] **[요한]** ✍️ **조합 맵 최종 컨펌 (D-24)**
  - 디렉터가 합리적 기본값으로 Notion 업데이트 완료:
    - 나뭇가지 × 2 → 목재
    - 질긴줄기 × 2 → 식물 섬유(끈)
    - 식물 섬유(끈) + 질긴줄기 → 깨끗한 천
    - 깨끗한 천 + 식물 섬유(끈) → 붕대
    - 돌맹이 = 머지 불가 (2단계 없음)
  - 변경 필요시 Notion `🎒 아이템` DB에서 직접 `조합법`·`결과물`·`머지 가능` 수정 후 요한이 `make data`. 디렉터한테 말해주면 배선.

## 대기: 4차 세션 잔여

- [ ] **[요한]** ✍️ **시트 `베이스탐험카드` 탭에 `rest`(휴식) 행 추가** (D-23 SSOT 정리)
  - 디렉터는 `REST_FALLBACK` 상수로 런타임 동작을 이미 구현했으나, SSOT 원칙상 시트에도 반영 필요.
  - 방법:
    1. 시트 [`베이스탐험카드`](https://docs.google.com/spreadsheets/d/1iS4Lmjx32w0Mu527foFtc8pQn3pw6APcQYnvcKdVM9o) 탭 열기.
    2. 마지막 row 아래 새 행 추가.
    3. 값(컬럼 순서는 기존 row 참조): `id=rest`, `name=휴식`, `count=1`, `effect=음식 1개 소비 · 회복 x2`, `detection=-10`, `time=1`, `consume=category:음식;scale:recover=2`, `extra_effect=`(빈칸).
    4. 헤더에 `time`/`consume`/`extra_effect` 컬럼이 없으면 함께 추가.
    5. 저장 → `make data` → 커밋.

- [ ] **[요한]** ✍️ **시트 `베이스탐험카드` 탭에서 `find_weapon` 행 수동 삭제** (3차 세션부터 대기중)
  - 디렉터는 런타임 필터로 이미 손패에서 제거했으나, **시트 실 row는 남아 있음**.
  - 방법: 시트 탭 → row 번호 우클릭 → "Delete row" → 저장 → `make data`.

---

## 4차 세션 대기 (원본 — 참고용)

- [ ] **[요한]** 🧪 **QA: 4차 세션(손패 5장 + 휴식 카드 + D-23 프레임워크) 검증**
  - `localhost:8765` 로드(이미 서버 떠있음) → 아래 항목 확인:
    - 손패가 5장까지 채워지고 한 줄에 나란히 표시된다. `손패 N/5` 카운터 표시.
    - "휴식" 카드가 덱/손패에 등장한다(폴백 상수로 런타임 보강된 카드 — 시트 row 추가 전까지).
    - 음식(버섯/산딸기)이 가방에 없는 상태에서 휴식 카드 클릭 → "지금 쓸 수 있는 아이템이 없다" 메시지 + 카드 미소모.
    - 음식이 있으면 휴식 클릭 → 모달에 음식만(돌맹이/나뭇가지 등 제외) 나열 → 선택 → 배고픔이 원래 +1이 아니라 **+2**로 들어간다(스케일 적용 확인).
    - 카드 시간 +1, 발각률 -10(0 미만이면 0에 clamp).
  - QA 이상 있으면 디렉터에게. 이상 없으면 "시트에 휴식 row 추가" 수동 작업 진행.

> 💡 4차 세션 시트 pending 2건(`rest` row 추가, `find_weapon` row 삭제)은 2026-04-24 기준 이미 반영 완료. `base_cards.json`에 rest 존재 + find_weapon 없음 검증.

## 요한 확정 대기 (디렉터 제안 중)

- _(없음 — "음식 카테고리 옵션 유지" 확정(D-21)으로 항목 닫힘.)_

## 완료 (2026-04-24, 10차 세션 — 시트 pending 일괄 청소)

- [x] **[디렉터]** gspread 서비스 계정 API로 시트 4건 수정 일괄 실행:
  - 시트 `전투카드` 탭: `count` 컬럼 신설 + 값 채움(회피=3, 나머지=1) + `throw_spear` row 신규 추가(damage=4, success=100, requirement=나무창, count=1).
  - 시트 `사냥감` 탭: `habitat` 컬럼 신설 + 1단계 9종 값 채움 (토끼·쥐·뱀·개구리=덤불, 다람쥐·도롱뇽=숲, 메추라기새·메뚜기=평원, 게=시냇물). 2단계 7종은 공란(후속 이터레이션).
  - 시트 `베이스탐험카드` 탭: `listen` row effect = "거리별 소리로 보스 감지 · 사냥감 흔적"로 갱신.
  - 시트 `특수카드` 탭 **신설** + `hunt_start` row (time=1, detection=2, effect, overflow=Y) 삽입.
- [x] **[디렉터]** `scripts/fetch_data.py` 업데이트: `SHEET_TABS`에 `"특수카드": "special_cards.json"`, `BROWSER_BUNDLE_KEYS`에 `special_cards.json → SPECIAL_CARDS` 추가.
- [x] **[디렉터]** `make data` 실행 완료 — `data/combat_cards.json`(6 rows, count 포함), `data/prey.json`(habitat 포함), `data/base_cards.json`(listen 갱신), `data/special_cards.json`(신규), `data/data.js` 재생성 확인. `[api]` 경로로 SSOT 동기 성공.
- [x] **[디렉터]** 런타임 폴백(`HUNT_START_CARD` 상수, `data/prey_habitat.json`, `inventory.js` meat 폴백) 정리는 이번 세션 범위 외. 시트 값이 JSON에 들어왔으므로 다음 이터레이션에서 런타임이 시트값을 우선 참조하도록 배선 필요.

## 완료 (2026-04-22, 4차 세션 — 손패 5장 + 휴식 + D-23 프레임워크)

- [x] **[디렉터]** 손패 상한 3 → 5 (`HAND_SIZE` 상수, `HandCards` 그리드 5열, 초기 드로우·보충·카운터 배선). D-22 기록.
- [x] **[디렉터]** `effectParser.js` 확장: `parseCardConsume`, `scaleEffect`, `matchesConsumeFilter` 신규 API + 기존 api export에 추가.
- [x] **[디렉터]** `CardItemConsumeModal` 신규 컴포넌트 (`index.html`).
- [x] **[디렉터]** `useCard` 재구조: `consume` 필드 보유 카드는 `openConsumeForCard` → 모달 → `resolveConsumeChoice`로 파이프라인. `finalizeCardUse` 헬퍼 분리.
- [x] **[디렉터]** 휴식 카드 폴백 상수 `REST_FALLBACK` + BASE_CARDS 보강 로직 (시트 row 생기면 자동 스킵). D-23 프레임워크 첫 구현체.
- [x] **[디렉터]** 브라우저 E2E 검증: 손패 5장 / 휴식 클릭 → 모달 → 버섯 선택 → 배고픔 +2 반영 / 콘솔 에러 0.

## 완료 (2026-04-22, 3차 세션 — 카테고리 확정 + push)

- [x] **[요한]** "음식" 카테고리 옵션 유지 결정 + 회복 효과 의무화 규칙 지시 → D-21 기록.
- [x] **[공동]** `git push origin main` — `bdedceb..f175550` 1커밋 push 성공(요한 "나머진 푸시하자" 명시, D-13 예외).

## 완료 (2026-04-22, 2차 세션 — QA 후 밸런스·UI 5건)

- [x] **[요한]** 구 Notion `🎲 타일 드롭 테이블` DB 수동 삭제. (`deleted` 플래그 확인.)
- [x] **[디렉터]** `find_weapon` 탐험 카드 런타임 제거: BASE_CARDS 필터링 + 폴백 상수 제거 + useCard 분기 삭제 (`index.html`).
- [x] **[디렉터]** `food` 독립 아이템 제거 (`inventory.js::ITEMS`).
- [x] **[디렉터]** `find_food` 카드 재배선: `dropTable.js::rollTileDrop(region, {forceCategory:'food'})` → 지역 음식재료 풀 추첨. `forceCategory` 옵션은 환경재료 루트(`env`)에도 쓸 수 있도록 일반화.
- [x] **[디렉터]** 방문 타일 지역 이름 오버레이 (`index.html::HexTile` + `REGION_LABEL` 매핑). visited 타일만.
- [x] **[디렉터]** 이동 시 발각률 +5% (`index.html::moveTo`) — `movedDetection`으로 chase 판정·boss.onPlayerMove 호출 일관화.
- [x] **[디렉터]** 브라우저 로드 검증 (Chrome MCP, 콘솔 에러 0, 동작 확인).

## 완료 (2026-04-22, 1차 세션)

- [x] **[디렉터]** Notion 등급 → 단계 통합 (D-14).
- [x] **[디렉터]** ITM-1~4 → 2단계, ITM-5~9 → 1단계 Notion 값 반영.
- [x] **[디렉터]** `data/items.raw.json` 수동 동기화 + `make data`로 `items.json`·`data.js` 재생성.
- [x] **[디렉터]** `data/drop_table.json` 수동 스냅샷 생성 (D-15) → 시트 탭 생성으로 **해결(superseded by 시트 SSOT)**.
- [x] **[디렉터]** `dropTable.js` → `window.TTD_DATA.DROP_TABLE.{regions, pool}` 우선 참조 + 하드코딩 폴백 유지.
- [x] **[공동]** 언커밋 작업물 커밋 (요한 "이후에 커밋하자" 명시 → D-13 예외 적용).
- [x] **[디렉터]** Notion `아이템 등급` select 레거시 옵션 "일반/희귀/전설" 삭제 확인 (요한 수동). 스키마 재조회 결과 `["1단계","2단계","3단계"]` 3개만 남음.
- [x] **[디렉터]** **시트 `드롭테이블`·`드롭풀` 탭 생성** (Chrome MCP 클립보드 붙여넣기 경로). 데이터 정합성 100%.
- [x] **[디렉터]** `make data` 실행 → `data/drop_table.json` 시트 값으로 재생성. `5 regions, 9 pool entries` 로그 확인.
- [x] **[디렉터]** Notion 아이템 DB `카테고리별`·`등급별` board view 유지 결정 (D-16, 요한 선호).
- [x] **[공동]** `git push origin main` — `1f5d70b..02c0e01` 3커밋 push 성공. origin/main 최신.

## 다음 이터레이션 후보 (기획 승인 대기)

### 0. 아이템 머지 시스템 (요한 예고, 다음 이터레이션 확정)

**요한 예고(2026-04-22)**: "이게 끝나면 머지 시스템을 만들거야".

**맥락**:
- 현재 `inventory.js`에 이미 머지 관련 로직이 **남아 있음** (`canMerge`, `mergeItems`, `confirmPlacement`의 `action:'merge'` 분기, `grade` 기반 1→2→3단계 승급).
- `material_low` / `material_mid` / `material_high`는 이 머지 경로의 산출물로 정의되어 있고, UI의 인벤토리 모달도 머지 미리보기(노란 점선)를 렌더하고 있다.
- 즉 시스템 코드는 살아있으나, **Notion 아이템 DB의 "머지 가능" 속성이 정리 과정에서 한번 사라졌다가 다시 체크박스로 복구되어** 시트·DB·코드의 연결이 느슨한 상태.
- 요한이 등급을 "희소도"에서 "파생 깊이(1/2/3단계)"로 재정의(D-14)했으므로, 머지의 의미도 다시 정의 필요:
  - 같은 1단계 재료 2개를 합쳐 2단계 파생 재료를 얻는 **조합과 유사한 규칙**인지
  - 아니면 같은 등급 아이템 슬롯 2개를 합쳐 "인벤 공간 절약 + 소수량 가산"만 하는 **그리드 UX 전용 규칙**인지
- 현재 `inventory.js` 코드는 후자(같은 `type` 기준 머지) + 전자 흔적(grade 승급) 혼재.

**이번 이터레이션 손대지 말 것**(요한 명시):
- 휴식/손패/프레임워크 구현 범위 외. 이번 세션에서는 파악·기록만.
- `inventory.js`의 머지 로직 건드리면 현재 재료 UI 회귀 위험(D-13 — 파괴적 변경 금지).

**다음 이터레이션 준비 질문 (요한에게)**:
- 머지의 게임 루프상 의미: 인벤 공간 압축인가, 파생 조합의 간단 버전인가?
- Notion 아이템 DB의 `머지 가능` 체크박스가 그대로 기준인가, 아니면 조합법(relation `재료`→`결과물`)이 SSOT가 되는가?
- 머지와 조합(C. 2단계 아이템)을 한 축으로 통합할 것인가, 별도 UX로 두 경로를 공존시킬 것인가?

### A. 드롭 테이블 시트 연결 — ✅ **완료**

### B. 전투 모드 UI (D-09 완료를 위한 전제)
**조건**: 기획 상세 (보스 조우 → 전투 진입 → UI 레이아웃 → 턴 구조).
**현재 존재하는 조각**:
- `combatExtraCards` state는 누적만 되고 사용처 없음.
- `CombatDeck.buildCombatDeck(base, extras)` / `consumeExtraCard(prev, cardId)` 유틸은 준비됨.
- 몬스터 1종(벨로시랩터) attack_pattern 파싱된 상태.
**분할 제안**:
1. 전투 진입 트리거 (보스와 같은 타일 도달 / 이벤트)
2. 전투 모달 UI (HP·몬스터 패턴 노출·손패)
3. 턴 진행 (유저 선택 → 몬스터 move → 피해 정산)
4. 전투 종료 (승리/도주/사망) + `combatExtraCards` 잔여 정리

### C. 2단계 아이템 (조합)
**전제**:
- Notion `조합법` 문자열 DSL 확정. 예: `나뭇가지 + 나뭇가지 → 목재` / `천 조각 x3 + 깨끗한 물 → 깨끗한 천 x1`.
- `effectParser.js` 옆에 `recipeParser.js` 신설 후보.
- 인벤토리 머지(같은 등급) vs 조합(서로 다른 재료)의 UX 분리.

### D. 빌딩 런타임 연결
`data/buildings.json`에 cost 파싱 완료. 실제 게임 루프에서 건축 명령·자원 차감·효과 적용은 미구현.

### E. 몬스터 다양화
현재 monsters.json에 벨로시랩터 1종. 난이도별 다른 보스·일반 몬스터 추가 여지. 시트 `몬스터` 탭에 row 추가만 하면 파이프라인은 자동 처리.

### F. 타일 이벤트 / 함정 상세화
`mapGenerator`가 `good`/`trap` 특수 타일을 배치 중이나 이벤트 테이블이 없음. D-03 원칙에 따라 시트 `이벤트` 탭 + DSL 파서로 확장 예정.

---

## 잊지 말 것 (리마인더)

- 언제든 `data/items.json`과 Notion이 어긋날 수 있음. 아이템 변경 시 `items.raw.json` 갱신 → `make data` 2단계 확인.
- 메모리 갱신 원칙: 작업 끝나면 이 파일·`project-state.md`를 건드린다. 쌓이기 전에.
- **Chrome MCP 시트 쓰기 레시피(D-17)**: type은 한글 IME 확정 문제 있음 → `navigator.clipboard.writeText(TSV)` + A1 클릭 + `cmd+v` 가 가장 안정적.
